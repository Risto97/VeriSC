set(VCS_HOME ${VCS_HOME})

set(SYSTEMC_HOME_${TAG} ${SYSTEMC_INSTALL_DIR})
set(SYSTEMC_UVM_HOME_${TAG} ${UVM_INSTALL_DIR})
set(SCV_HOME_${TAG} ${SCV_INSTALL_DIR})
set(FC4SC_HOME_${TAG} ${FC4SC_INSTALL_DIR})
set(ICSC_COMPILER_HOME_${TAG} ${ICSC_COMPILER_INSTALL_DIR})
set(SC_UVM_ENV_HOME_${TAG} ${SC_UVM_ENV_INSTALL_DIR})
set(VERILATOR_HOME_${TAG} ${VERILATOR_INSTALL_DIR})

include(${GROUP_INSTALL_DIR}/vcs_home_resolve.cmake)
