set(BOOST_HOME_${TAG} ${BOOST_INSTALL_DIR_SHARED})
set(ZLIB_HOME_${TAG} ${ZLIB_INSTALL_DIR})
set(GLOG_HOME_${TAG} ${GLOG_INSTALL_DIR})
set(BOOLECTOR_HOME_${TAG} ${BOOLECTOR_INSTALL_DIR})
set(CRYPTOMINISAT_HOME_${TAG} ${CRYPTOMINISAT_INSTALL_DIR})
set(LINGELING_HOME_${TAG} ${LINGELING_INSTALL_DIR})
set(CADICAL_HOME_${TAG} ${CADICAL_INSTALL_DIR})
set(CUDD_HOME_${TAG} ${CUDD_INSTALL_DIR})
set(Z3_HOME_${TAG} ${Z3_INSTALL_DIR})

set(CRAVE_HOME_${TAG} ${CRAVE_INSTALL_DIR})

